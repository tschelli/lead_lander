# School Landing Page Environment Variables
# Each deployment is scoped to a single school

# Required: School and Client IDs (from database)
NEXT_PUBLIC_SCHOOL_ID=school_northwood
NEXT_PUBLIC_CLIENT_ID=client_acme

# Required: API endpoint
NEXT_PUBLIC_API_BASE_URL=http://localhost:4000

# Optional: Default program to redirect to from root
# DEFAULT_PROGRAM_SLUG=medical-assistant
